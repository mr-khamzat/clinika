# Полный аудит КлиникСеть · 29 мая 2026 · Opus 4.7

## 🔴 CRITICAL (3 находки — фиксить немедленно)

1. **Hardcoded TG bot token в коде** — `backend/app/routers/support.py:25`
   `_DEFAULT_BOT_TOKEN = '8689519551:AAHeH7apn...'` — fallback если в БД пусто.
   → Отозвать в @BotFather + убрать константу.

2. **python-jose 3.3.0 — 5 CVE (JWT)** — `requirements.txt:9`
   PYSEC-2024-232/233, PYSEC-2025-185 (algorithm confusion + JWT bomb DoS). Вся auth на этой либе.
   → Обновить до 3.4.0+ или мигрировать на PyJWT.

3. **Слабый пароль super_admin** — `.env`
   `SUPERADMIN_PASSWORD=khamzat88712` (12 chars, имя+дата — словарный).
   → Сменить на 24+ char random, включить MFA.

## 🟠 HIGH (6 находок)

- **python-multipart 0.0.9** — 4 CVE (DoS через upload) → `>=0.0.27`
- **Pillow 10.4.0** — 5 CVE (CVE-2026-42310/42311 heap) → `>=12.2.0`
- **starlette 0.46.2** — CVE-2025-54121/62727 → `>=0.49.1`
- **weasyprint 62.3** — CVE-2025-68616 (PDF медкарты с ПДн) → `>=68.0`
- **CSP с 'unsafe-inline' и 'unsafe-eval'** — nginx config, нивелирует XSS-защиту
- **`localhost:5173` в prod CORS с credentials=True** — `.env ALLOWED_ORIGINS`. Локальная малварь у пациента может обратиться к API с его cookie.

## 🟡 MEDIUM (5 находок)

- JWT HS256 (shared secret) — мигрировать на RS256/EdDSA
- Refresh token не ротируется при /auth/refresh (украденный refresh живёт 30 дней)
- HSTS без preload + не в hstspreload.org
- Jinja2 3.1.4 — CVE-2024-56326 (sandbox escape)
- **ПДн пациентов хранятся plaintext в БД** (152-ФЗ для мед.данных требует усиленной защиты)
- **Plain-text платёжные секреты** в `models/payments_clinic.py:13,136,211` — TODO: Fernet.encrypt не сделан

## 🟢 LOW (5 находок)

- Webhook signature только если secret задан, нет timestamp → replay
- pip 24.0, pytest 8.3.4 с CVE
- File upload: trust client content-type (нет magic bytes)
- Exim4+Dovecot exposed (25/110/143/465/993/995) — если почта не нужна, отключить

---

## 🏗️ ИНФРАСТРУКТУРА

**Критично:**
- nginx позволяет TLSv1/1.1 на блоках `212.57.118.126` и `xn--e1afagcdp8ak4h.xn--p1ai` — downgrade-атаки
- BIND named слушает 53 на публичном IP — возможный open resolver / DNS amplification
- redis-exporter unhealthy 6 дней — мониторинг Redis сломан

**Хорошо:**
- TLS Let's Encrypt до 6 июля 2026 (38 дней, certbot настроен)
- UFW активен (DROP по умолчанию), 22/80/443 — только то что нужно
- Бэкапы: ежедневные GPG-шифрованные + еженедельный test-restore — зрелая автоматизация
- Диск 21% (24G/119G), RAM 9Gi free, swap 0/4G

**Что обновить:**
- 33 пакета Ubuntu ждут apt upgrade
- cadvisor 2 года старый

---

## 🗄️ БАЗА ДАННЫХ (38 MB, всё чисто)

- 426 FK, **122 без индекса** (28%) — на росте данных будет тормоз DELETE/UPDATE/JOIN
- 0 orphan rows, 0 дубликатов email/slug, единая alembic head `npssurvey01`
- 0 `*_bak/_old/_tmp` таблиц
- Topas: services 8MB, ltv_snapshots 5MB, refresh_tokens 632KB

---

## ⚙️ BACKEND

**Активные runtime-ошибки:**
- `TypeError: offset-naive vs offset-aware datetime` в chat_sla_job
- `quota_flush_job` FK violation для 2 несуществующих tenants
- `tg poll error` — owner-bot poll цикл шумит

**Не подключенные / лишние:**
- `routers/patient_documents_v2.py` — name conflict при include
- `routers/referrals_cross.py` — legacy, заменён `franchise_referral.py`
- Дубль чата: `patient_chat.py` (старый) + `patient_chat_threads.py` (новый) — оба активны
- 5 TODO для шифрования платёжных секретов (Fernet)
- 9 файлов >1500 строк (admin.py 2109, staff_chat.py 1525, director.py 1506)

**Удалить:** `backend/requirements.txt.bak`, `backend/app/main.py.bak.quota01`

---

## 🎨 FRONTEND

**К удалению (SURE — 0 импортов):**
- `pages/Login.jsx` (186 строк) — заменён единым входом через Landing
- `pages/WikiViewer.jsx` (367 строк) — заменён Wiki+WikiArticle
- `components/subscription/PlanCard.jsx` (159 строк) — заменён PlanCardV2
- `TrialBanner`, `ResponsiveTable`, `PatientCardSidebar`, `SmartReplyButton`, `SLAFilter`, `NPSCard`, `SwitchContextBanner` — ~580 строк
- 2 .bak файла в pages/ + 4 .env.bak в /opt/clinika/

**Probably:**
- `Dashboard.jsx` (331 строки) — импортится, но в Routes не используется

**Метрики:**
- 0 console.log в коде (отлично)
- 53 TODO (все — миграция на design-system)
- 9 файлов >1500 строк: `AdminLayout 9004`, `PatientCabinet 3825`, `StaffChat 2806`...

---

## 📦 ЗАВИСИМОСТИ

**21 CVE Python + 7 CVE npm (1 critical / 3 high / 3 moderate)**

**Major-отставания:**
- vite 5.4 → 8.0 (3 major!)
- react 18.3 → 19.2
- react-router 6 → 7
- tailwindcss 3 → 4
- redis 5.0 → 8.0
- pydantic 2.10 → 2.13
- material-symbols отстал на 20 версий

**Pin policy:** 5 пакетов в requirements.txt через `>=` (риск дрейфа) — pin строго.

---

## 🎯 ПОРЯДОК ФИКСОВ

**Эта неделя (CRITICAL):**
1. Отозвать TG-токен 8689... в BotFather, удалить из кода
2. `pip install python-jose>=3.4.0 python-multipart>=0.0.27 pillow>=12.2.0 starlette>=0.49.1 weasyprint>=68.0 jinja2>=3.1.6`
3. Сменить SUPERADMIN_PASSWORD на 24+ char random
4. Убрать `localhost:5173` из prod ALLOWED_ORIGINS
5. Отключить TLSv1/1.1 в nginx
6. Включить шифрование платёжных секретов (Fernet) в payments_clinic.py

**Этот месяц (HIGH/MEDIUM):**
7. CSP без `unsafe-eval`, добавить HSTS preload
8. Refresh-token rotation
9. PII encryption at rest для medical_record
10. Починить chat_sla_job (datetime tz) + quota_flush (FK)
11. Удалить ~2000 строк мёртвого фронта + 2 backend .bak
12. Закрыть DNS-port 53 наружу (или ограничить рекурсию BIND)

**Backlog (LOW):**
13. Создать индексы на 122 FK
14. Обновить cadvisor + apt upgrade
15. Magic bytes валидация uploads
16. Webhook timestamp + replay protection

---

# Сессия 2 — Wave B (29 мая, ~04:30 UTC)

## Применено

**Frontend deps (CVE):**
- jspdf 2.5.2 → **4.2.1** (critical + high CVE закрыты)
- jspdf-autotable 3.x → **5.0.8** (новый API: autoTable(doc, opts) — у нас уже был)
- xlsx 0.18.5 (npm) → **SheetJS CDN 0.20.3** (CVE-2023-30533 + ReDoS)
- vite 5.3 → **5.4.20** (закрыто несколько minor CVE; major 8.x отложен — CVE-2026-39365 нас не затрагивает)
- axios 1.15 → 1.16.1
- font: 'Roboto' захардкожен в PDF-отчёте (защита от deprecation в jspdf 4)

**npm audit:** 7 vulns (1 critical / 3 high / 3 moderate) → **2 moderate** (только dev-only esbuild через vite — прод не затронут)

**Backend & infra (Wave A, ранее):**
- Refresh-token rotation (миграция  применена, фронт уже rotation-ready)
- Fernet шифрование payment/OFD секретов (миграция )
- 6 уязвимых Python-пакетов (jose/multipart/pillow/starlette/weasyprint/jinja2)
- TLSv1/1.1 off, HSTS preload, CSP без unsafe-eval
- 2 endpoint-bugfix: SQL syntax (franchise-owner), datetime tz (chat_sla), FK violation (quota_flush)
- ~2000 строк dead frontend в архив

## Отложено (требует окна или согласования)

| Что | Почему отложил |
|---|---|
| nginx 1.28→1.30 + 21 ispmanager пакет | Перезапускает фронт сети — окно обслуживания |
| **PII at-rest шифрование (медданные)** | Затрагивает 6–10 моделей + сервисы. Сейчас все основные таблицы (, , , , ) **ПУСТЫЕ** — идеальное окно для миграции. Готов рецепт по 3 этапам |
| PII Этап 2 (контакты с blind-hash) | Затрагивает login/OTP/family-linking, риск сломать аутентификацию. Отдельная итерация |
| Vite 5→8 | CVE не наша (только 6.0+), нет смысла рисковать ради 10–30× ускорения билда |
| Пароль super_admin | По просьбе пользователя |

## Бэкапы

-  (перед Wave A)
-  +  (перед Wave B)
-  — все убранные .bak, .env-backup, legacy router
