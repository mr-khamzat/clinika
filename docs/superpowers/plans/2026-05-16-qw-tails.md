# Quick Wins хвосты — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development или superpowers:executing-plans.

**Goal:** Реализовать drag&drop файлов, quoted reply, кнопку Web Push subscribe в чате клиники.

**Architecture:** 1 миграция + 1 upload endpoint + расширение serialize_message + UI компоненты. Backend pluggable (uploads в существующую папку `/app/uploads/`).

**Tech Stack:** FastAPI multipart, SQLAlchemy alembic, React 18.

---

## Task 1: Backend — миграция reply_to_id + upload endpoint + расширение serializer

**Files:**
- Modify: `backend/app/models/chat.py` (+reply_to_id в ChatMessage)
- Create: `backend/alembic/versions/2026_05_16_qw01_reply_to.py`
- Modify: `backend/app/routers/clinic_chat.py` (+POST /threads/{id}/files, +reply_to_id в POST messages)
- Modify: `backend/app/services/chat_service.py` (расширить serialize_message — reply_to объект)
- Create: `backend/tests/test_qw_tails.py` (3 теста)

- [ ] **Step 1: Миграция qw01_reply_to**

`backend/alembic/versions/2026_05_16_qw01_reply_to.py`:
```python
"""qw01: reply_to_id в chat_messages"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID

revision = 'qw01_reply_to'
down_revision = 'tel01_telephony'
branch_labels = None
depends_on = None


def upgrade():
    op.add_column('chat_messages',
        sa.Column('reply_to_id', UUID(as_uuid=True),
            sa.ForeignKey('chat_messages.id', ondelete='SET NULL'),
            nullable=True, index=True))


def downgrade():
    op.drop_column('chat_messages', 'reply_to_id')
```

- [ ] **Step 2: Расширить модель ChatMessage**

В `backend/app/models/chat.py` найти класс `ChatMessage`, добавить (рядом с другими полями):
```python
    reply_to_id: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True),
        ForeignKey('chat_messages.id', ondelete='SET NULL'),
        nullable=True, index=True,
    )
```
(Импорты `UUID`, `ForeignKey` уже должны быть.)

- [ ] **Step 3: Тесты**

`backend/tests/test_qw_tails.py`:
```python
import io
import uuid
import pytest
from unittest.mock import AsyncMock, MagicMock


def test_reply_to_field_on_model():
    """Колонка reply_to_id присутствует в ChatMessage."""
    from app.models.chat import ChatMessage
    cols = [c.name for c in ChatMessage.__table__.columns]
    assert "reply_to_id" in cols


@pytest.mark.asyncio
async def test_serialize_message_includes_reply_preview():
    """serialize_message при наличии reply_to_id отдаёт preview оригинала."""
    from app.services.chat_service import serialize_message
    original = MagicMock(
        id=uuid.uuid4(), body="Длинный оригинальный текст " * 20,
        sender_type="patient", sender_id=uuid.uuid4(),
    )
    msg = MagicMock(
        id=uuid.uuid4(),
        thread_id=uuid.uuid4(),
        sender_type="doctor",
        sender_id=uuid.uuid4(),
        body="Мой ответ",
        attachments=None,
        read_at=None,
        created_at=None,
        reply_to_id=original.id,
    )
    # serialize_message_with_reply(msg, reply_to=original) — расширенный сериализатор
    from app.services.chat_service import serialize_message_with_reply
    out = serialize_message_with_reply(msg, reply_to=original)
    assert out.get("reply_to") is not None
    rt = out["reply_to"]
    assert rt["id"] == str(original.id)
    assert len(rt["body_preview"]) <= 100
    assert rt["sender_type"] == "patient"


def test_serialize_message_without_reply():
    """Если reply_to_id=None — поля reply_to нет."""
    from app.services.chat_service import serialize_message_with_reply
    msg = MagicMock(
        id=uuid.uuid4(), thread_id=uuid.uuid4(),
        sender_type="doctor", sender_id=None, body="нет ответа",
        attachments=None, read_at=None, created_at=None,
        reply_to_id=None,
    )
    out = serialize_message_with_reply(msg, reply_to=None)
    assert out.get("reply_to") is None


def test_upload_size_limit_constant():
    """Лимит upload 50MB."""
    from app.routers.clinic_chat import MAX_UPLOAD_SIZE
    assert MAX_UPLOAD_SIZE == 50 * 1024 * 1024
```

- [ ] **Step 4: Сериализатор `serialize_message_with_reply`**

В `backend/app/services/chat_service.py` добавить:
```python
def serialize_message_with_reply(m, reply_to=None) -> dict:
    """Сериализация сообщения с опциональным preview ответа.

    Args:
      m: ChatMessage
      reply_to: ChatMessage | None — оригинал на который отвечают
    """
    base = serialize_message(m)
    base["reply_to_id"] = str(m.reply_to_id) if m.reply_to_id else None
    if reply_to is not None:
        preview = (reply_to.body or "")[:100]
        base["reply_to"] = {
            "id": str(reply_to.id),
            "body_preview": preview,
            "sender_type": reply_to.sender_type,
        }
    else:
        base["reply_to"] = None
    return base
```

- [ ] **Step 5: Upload endpoint**

В `backend/app/routers/clinic_chat.py` добавить (после существующих endpoints):
```python
import os
from pathlib import Path
from fastapi import UploadFile, File

MAX_UPLOAD_SIZE = 50 * 1024 * 1024  # 50 MB
UPLOAD_DIR = Path("/app/uploads/clinic-chat")


@router.post("/threads/{thread_id}/files", status_code=201)
async def upload_thread_file(
    thread_id: uuid.UUID,
    file: UploadFile = File(...),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    _ensure_clinic_role(user)
    # Проверка доступа к треду
    rt = await db.execute(select(ChatThread).where(ChatThread.id == thread_id))
    th = rt.scalar_one_or_none()
    if not th:
        raise HTTPException(404, "Thread not found")
    allowed = await _user_clinic_ids(db, user)
    if th.clinic_id not in allowed:
        raise HTTPException(403, "Нет доступа к этому треду")

    # Размер
    data = await file.read()
    if len(data) > MAX_UPLOAD_SIZE:
        raise HTTPException(413, f"Файл слишком большой (макс. {MAX_UPLOAD_SIZE // 1024 // 1024} МБ)")

    # Сохранение
    UPLOAD_DIR.mkdir(parents=True, exist_ok=True)
    thread_dir = UPLOAD_DIR / str(thread_id)
    thread_dir.mkdir(exist_ok=True)
    safe_name = file.filename.replace("/", "_").replace("\\", "_")[:200]
    ext = Path(safe_name).suffix
    file_id = uuid.uuid4().hex
    path = thread_dir / f"{file_id}{ext}"
    path.write_bytes(data)

    public_url = f"/uploads/clinic-chat/{thread_id}/{file_id}{ext}"
    return {
        "url": public_url,
        "name": safe_name,
        "mime": file.content_type or "application/octet-stream",
        "size": len(data),
    }
```

- [ ] **Step 6: Расширить POST /messages — поддержка reply_to_id**

Найти существующий endpoint `POST /threads/{thread_id}/messages` в `clinic_chat.py`. В Pydantic-схеме (вероятно `MessageIn` или подобной) добавить:
```python
class MessageIn(BaseModel):
    body: str = Field(...)
    attachments: Optional[List[Any]] = None
    reply_to_id: Optional[uuid.UUID] = None  # ← НОВОЕ
```

В обработчике, перед `cs.add_message(...)`, если `body.reply_to_id` — проверить что оригинал в этом же треде:
```python
if body.reply_to_id:
    orig = (await db.execute(
        select(ChatMessage).where(
            ChatMessage.id == body.reply_to_id,
            ChatMessage.thread_id == thread_id,
        )
    )).scalar_one_or_none()
    if not orig:
        raise HTTPException(400, "Оригинал ответа не найден или из другого треда")
```

И в сохраняемое сообщение передать `reply_to_id=body.reply_to_id`. Найти `cs.add_message` или эквивалентный путь.

- [ ] **Step 7: Применить миграцию, rebuild, тесты**

```bash
sshpass scp files root@...
sshpass ssh root@... 'cd /opt/clinika && docker compose exec -T clinika-backend alembic upgrade head && docker compose build clinika-backend && docker compose up -d clinika-backend && sleep 8 && docker compose exec -T clinika-backend pytest tests/test_qw_tails.py -v'
```
Expected: `Running upgrade tel01_telephony -> qw01_reply_to`, потом 4 passed.

- [ ] **Step 8: Commit**

```bash
git add backend/app/models/chat.py backend/app/services/chat_service.py backend/app/routers/clinic_chat.py backend/alembic/versions/2026_05_16_qw01_reply_to.py backend/tests/test_qw_tails.py
git -c commit.gpgsign=false commit -m "feat(qw-tails): reply_to_id + upload endpoint + 4 теста"
```

---

## Task 2: Frontend — drag&drop + quoted reply + push button

**Files:**
- Modify: `frontend/src/sections/ClinicChatSection.jsx`
- Modify: `frontend/src/components/chat/MessageBubble.jsx`

- [ ] **Step 1: SCP оба файла**

```bash
sshpass scp root@...:/opt/clinika/frontend/src/sections/ClinicChatSection.jsx /tmp/...
sshpass scp root@...:/opt/clinika/frontend/src/components/chat/MessageBubble.jsx /tmp/...
```

- [ ] **Step 2: State для drag&drop + replyingTo + push в ClinicChatSection**

В импорты добавить:
```jsx
import { enableWebPush, disableWebPush, getPushPermissionState } from '../lib/webPush'
```

Рядом с существующими state добавить:
```jsx
const [isDragging, setIsDragging] = useState(false)
const [pendingAttachments, setPendingAttachments] = useState([])
const [replyingTo, setReplyingTo] = useState(null)  // {id, body_preview, sender_type}
const [pushState, setPushState] = useState('default')  // default|granted|denied|unsupported

useEffect(() => {
  getPushPermissionState().then(setPushState)
}, [])
```

- [ ] **Step 3: Drag&drop handlers**

В компонент добавить:
```jsx
const handleDrop = async (e) => {
  e.preventDefault()
  setIsDragging(false)
  if (!activeId) return
  const files = Array.from(e.dataTransfer?.files || [])
  for (const file of files) {
    const fd = new FormData()
    fd.append('file', file)
    try {
      const r = await api.post(`/clinic/chat/threads/${activeId}/files`, fd, {
        headers: { 'Content-Type': 'multipart/form-data' },
      })
      setPendingAttachments(prev => [...prev, r.data])
    } catch (err) {
      toast?.(err?.response?.data?.detail || 'Не удалось загрузить файл', 'error')
    }
  }
}
const handleDragOver = (e) => { e.preventDefault(); setIsDragging(true) }
const handleDragLeave = (e) => { e.preventDefault(); setIsDragging(false) }
```

Найти контейнер сообщений (с классом `chat-scroll` или подобным) и обернуть props:
```jsx
<div
  className="chat-scroll flex-1 overflow-y-auto p-3"
  onDragOver={handleDragOver}
  onDragLeave={handleDragLeave}
  onDrop={handleDrop}
  style={{ position: 'relative', ... }}
>
  {/* существующее содержимое */}
  {isDragging && (
    <div style={{
      position: 'absolute', inset: 0, zIndex: 50,
      background: 'rgba(0,151,167,.15)', border: '3px dashed #0097A7',
      display: 'grid', placeItems: 'center',
      fontSize: 16, fontWeight: 600, color: '#0097A7',
      pointerEvents: 'none',
    }}>
      📎 Отпустите чтобы прикрепить
    </div>
  )}
</div>
```

- [ ] **Step 4: Pending attachments preview над textarea**

Найти input-area и над textarea добавить:
```jsx
{pendingAttachments.length > 0 && (
  <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', padding: '6px 8px' }}>
    {pendingAttachments.map((a, i) => (
      <div key={i} style={{
        padding: '4px 8px', borderRadius: 8, fontSize: 11,
        background: 'var(--bg-1, #f1f5f9)', display: 'flex', gap: 4, alignItems: 'center',
      }}>
        <span>📎</span>
        <span style={{ maxWidth: 120, overflow: 'hidden', textOverflow: 'ellipsis' }}>{a.name}</span>
        <button onClick={() => setPendingAttachments(prev => prev.filter((_, j) => j !== i))}
                style={{ background: 'transparent', border: 'none', cursor: 'pointer', color: 'var(--fg-3)' }}>✕</button>
      </div>
    ))}
  </div>
)}
```

- [ ] **Step 5: Reply UI над textarea**

Над textarea (но под pendingAttachments) добавить:
```jsx
{replyingTo && (
  <div style={{
    display: 'flex', alignItems: 'center', gap: 8,
    padding: '6px 10px', margin: '0 8px 4px',
    background: 'var(--bg-1, #f1f5f9)',
    borderLeft: '3px solid var(--accent, #0097A7)',
    borderRadius: 8,
    fontSize: 12,
  }}>
    <span className="material-symbols-outlined" style={{ fontSize: 14, color: 'var(--accent, #0097A7)' }}>reply</span>
    <span style={{ flex: 1, color: 'var(--fg-2, #475569)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
      Ответ: {replyingTo.body_preview}
    </span>
    <button onClick={() => setReplyingTo(null)}
            style={{ background: 'transparent', border: 'none', cursor: 'pointer', color: 'var(--fg-3)' }}>✕</button>
  </div>
)}
```

- [ ] **Step 6: Передача reply_to_id и attachments при отправке**

Найти функцию `send` или `sendMessage`. В payload добавить:
```jsx
await api.post(`/clinic/chat/threads/${activeId}/messages`, {
  body,
  attachments: pendingAttachments.length ? pendingAttachments : undefined,
  reply_to_id: replyingTo?.id,
})
setPendingAttachments([])
setReplyingTo(null)
```

- [ ] **Step 7: Push subscribe button**

В шапке треда (рядом с soundEnabled toggle):
```jsx
{pushState !== 'unsupported' && (
  <button
    onClick={async () => {
      if (pushState === 'granted') {
        const r = await disableWebPush()
        if (r.ok) setPushState('default')
      } else {
        const r = await enableWebPush()
        if (r.ok) {
          setPushState('granted')
          toast?.('Уведомления включены', 'success')
        } else if (r.reason) {
          toast?.(r.reason, 'error')
        }
      }
    }}
    className="grid place-items-center"
    style={{
      width: 36, height: 36, borderRadius: 10,
      background: 'var(--bg-1, #f1f5f9)',
      color: pushState === 'granted' ? 'var(--accent, #0097A7)' : 'var(--fg-3, #94a3b8)',
    }}
    title={pushState === 'granted' ? 'Push-уведомления: вкл' : 'Включить push-уведомления'}
    aria-label="Push notifications"
  >
    <span className="material-symbols-outlined" style={{ fontSize: 20, fontVariationSettings: pushState === 'granted' ? "'FILL' 1" : "'FILL' 0" }}>
      {pushState === 'granted' ? 'notifications_active' : 'notifications'}
    </span>
  </button>
)}
```

- [ ] **Step 8: В renderedMessages передавать onReply**

Найти `<MessageBubble onReact={...} />` и добавить:
```jsx
<MessageBubble
  ...
  onReact={active?.thread?.status !== 'closed' ? doReact : undefined}
  onReply={active?.thread?.status !== 'closed' ? (msg) => setReplyingTo({
    id: msg.id,
    body_preview: (msg.body || '').slice(0, 100),
    sender_type: msg.sender_type,
  }) : undefined}
/>
```

- [ ] **Step 9: MessageBubble — кнопка Reply + render reply_to**

В `frontend/src/components/chat/MessageBubble.jsx`:
- Принять prop `onReply`
- Добавить кнопку «↩» в reactions-bar рядом с add_reaction:
```jsx
{onReply && (
  <button
    type="button"
    onClick={() => onReply(message)}
    title="Ответить"
    aria-label="Ответить"
    style={{
      display: 'inline-grid', placeItems: 'center',
      width: 22, height: 22, borderRadius: 8,
      background: 'var(--bg-1, #f1f5f9)',
      border: '1px solid var(--border, #e2e8f0)',
      cursor: 'pointer', color: 'var(--fg-3, #94a3b8)',
    }}
  >
    <span className="material-symbols-outlined" style={{ fontSize: 14 }}>reply</span>
  </button>
)}
```
- В render — если `message.reply_to` есть, до body показывать цитату:
```jsx
{message.reply_to && (
  <div style={{
    padding: '6px 8px', marginBottom: 6, borderRadius: 8,
    borderLeft: '3px solid var(--accent, #0097A7)',
    background: 'rgba(0,151,167,.06)',
    fontSize: 12,
    color: isOwn ? 'rgba(255,255,255,.85)' : 'var(--fg-2, #475569)',
    fontStyle: 'italic',
    cursor: 'pointer',
  }}
  onClick={() => {
    const el = document.querySelector(`[data-msg-id="${message.reply_to.id}"]`)
    if (el) el.scrollIntoView({ behavior: 'smooth', block: 'center' })
  }}
  title="Перейти к оригиналу">
    ↩ {message.reply_to.body_preview}
  </div>
)}
```
- На баббл-обёртке добавить `data-msg-id={message.id}` для якоря.

- [ ] **Step 10: Build + smoke + commit**

```bash
scp + ssh + docker compose build clinika-frontend && up -d && curl 200
git add frontend/src/sections/ClinicChatSection.jsx frontend/src/components/chat/MessageBubble.jsx
git -c commit.gpgsign=false commit -m "feat(qw-tails): drag&drop + quoted reply + push subscribe UI"
```

---

## Task 3: Финальный smoke + TG

- [ ] **Step 1: pytest run**

```bash
docker compose exec -T clinika-backend pytest tests/test_qw_tails.py tests/test_telephony.py -v 2>&1 | tail -5
```

- [ ] **Step 2: TG report через стандартный паттерн** (см. предыдущие батчи)

---

## Self-Review

**Spec coverage:**
- §1 Drag&drop → Task 1 step 5 (endpoint) + Task 2 steps 3-4 (UI)
- §2 Quoted reply → Task 1 steps 1-6 (миграция + endpoint + сериализатор) + Task 2 steps 5-9 (UI)
- §3 Push UI → Task 2 step 7

**Type consistency:**
- `serialize_message_with_reply(m, reply_to=None) -> dict` — определён Task 1 step 4
- `MAX_UPLOAD_SIZE` константа — Task 1 step 5, тест step 3
- `MessageIn.reply_to_id` — Task 1 step 6
- `replyingTo: {id, body_preview, sender_type}` shape — одинаково в ClinicChatSection и MessageBubble
- `pendingAttachments[]: {url, name, mime, size}` — frontend Task 2 step 3 (формат от backend Task 1 step 5)

OK.
