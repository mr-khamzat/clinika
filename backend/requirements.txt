fastapi==0.115.14
# starlette закреплён явно (#38): fastapi 0.115.x требует starlette>=0.40.0,<0.42.0 —
# дублируем диапазон, чтобы транзитивная зависимость была видимой и воспроизводимой.
starlette>=0.40.0,<0.42.0
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
pydantic==2.10.6
pydantic-settings==2.7.1
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
# CVE-фикс (#8): CVE-2024-53981 (DoS при разборе multipart) исправлен в 0.0.18.
# Прод (Wave A/B) уже на 0.0.20 — это >= 0.0.18, т.е. CVE закрыт и версия выше;
# сохраняем прод-версию (не понижаем), совместимо с fastapi 0.115 (требует >=0.0.7).
python-multipart==0.0.20
redis==5.0.4
qrcode[pil]==7.4.2
# CVE-фикс (#8): подъём с 10.4.0 до 11.3.0 (фикс CVE в декодере/рендере шрифтов); 11.x совместим с qrcode[pil]==7.4.2, не связан с FastAPI/pydantic
Pillow==11.3.0
httpx==0.28.1
python-telegram-bot==21.2
aiofiles==23.2.1
fastapi-limiter==0.1.6
psutil==5.9.8
docker==7.1.0
pywebpush==2.3.0
# Фикс дрейфа версий (#56): верхний потолок по мажору (>=floor,<next-major),
# чтобы reproducible-сборка не подтянула breaking-релиз. Точный лок — через
# pip-compile в ops (не в этой правке, без установки).
apscheduler>=3.10.4,<4
structlog>=24.0.0,<25
prometheus-client>=0.20.0,<1
tenacity>=8.2.0,<9
geoip2==4.8.0
maxminddb==2.6.0
sentry-sdk[fastapi]==2.20.0
email-validator==2.1.1
aiosmtplib==3.0.1
weasyprint==66.0
pydyf==0.11.0
openpyxl==3.1.2
icalendar==5.0.13
# CVE-фикс (#66): 3.1.4 уязвим (CVE-2024-56201/56326, CVE-2025-27516).
# 3.1.6 — патч-релиз той же минорной ветки, без изменения API. weasyprint совместим.
jinja2==3.1.6
# Фикс дрейфа версий (#56): прод-флор >=0.45 сохранён, добавлен потолок по мажору <1.
holidays>=0.45,<1
# anthropic: прод-флор >=0.45.0 сохранён; фиксируем мажор 0.x (#56) — частые
# breaking-изменения SDK; AsyncAnthropic/messages.create стабильны в 0.x.
anthropic>=0.45.0,<1
